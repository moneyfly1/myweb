# 订阅列表管理功能说明

## 功能概述

订阅列表管理是管理员后台的核心功能，用于管理系统中所有用户的订阅信息。该功能提供了订阅信息的查看、搜索、筛选、编辑、重置等完整操作。

## 访问路径

- **管理员后台** → **订阅管理** → **订阅列表**

## 主要功能

### 1. 订阅信息展示

订阅ID、用户信息、订阅地址、设备限制、当前设备（当前数/限制数）、到期时间、状态、操作。

### 2. 搜索与批量操作

关键词搜索（QQ、订阅地址）、状态/到期时间筛选；批量删除、批量启用/禁用、批量重置、批量发送订阅邮件。

### 3. 单个订阅操作

查看详情、编辑订阅（设备限制、到期时间、状态）、重置订阅、发送订阅邮件、快捷添加时间（+1天/+7天/+30天等）、登录为用户。

### 4. 设备数量限制原理

- **设备识别**：基于 User-Agent、IP 等生成设备哈希
- **设备漫游**：同一设备在不同网络下会更新 IP/Hash，不重复计为多设备
- **限制逻辑**：未超限可添加新设备；达到或超过限制时，采用「最近使用优先」策略，只允许最近使用的 N 个设备使用订阅
- **设备优先级**：按最后访问时间排序，取前 DeviceLimit 个为允许列表，其余返回错误节点

## 相关功能

- [设备管理功能说明](./设备管理功能说明.md)
- [用户列表管理功能说明](./用户列表管理功能说明.md)
- [节点管理功能说明](./节点管理功能说明.md)

## 订阅分发机制

### 1. 客户端类型自动识别

系统通过请求的 **User-Agent** 自动识别客户端类型，返回对应格式的订阅：

| 客户端 | 订阅格式 |
|--------|---------|
| Clash / Clash Meta / Stash | YAML（含代理组与分流规则） |
| Surge | .conf |
| Sing-box | JSON |
| QuantumultX / Loon | .conf |
| Shadowrocket / v2rayN / 其他 | Base64 链接列表 |

### 2. 客户端版本过滤（新协议按客户端推送）

自动按客户端**类型与版本**过滤其不支持的新协议（如老版 Clash 不推送 VLESS/Reality/Hysteria2），避免订阅解析失败。详见：[协议过滤与客户端版本过滤说明](../配置/协议过滤与客户端版本过滤说明.md)。

### 3. 协议过滤与 exclude

- 系统级协议白名单（设置 → 协议过滤）控制各环境推送范围；
- 订阅 URL 可附加 `&exclude=协议1,协议2` 手动排除指定协议（如 `&exclude=anytls`）。

### 4. 按 IP 地区分发（可选）

系统内置 GeoIP 能力，可配置按用户来源地区推送不同节点（如海外用户优先推送海外节点）。

### 5. 订阅缓存

订阅配置与节点列表通过 Redis 缓存（配置 REDIS_ADDR 后启用），节点更新或配置变更后自动失效重建，兼顾实时性与性能。
