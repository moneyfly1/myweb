# 宝塔面板部署指南

## 📋 部署前准备

### 1. 确认项目位置
项目已复制到：`/www/wwwroot/dy.moneyfly.top`

### 2. 检查系统要求
- ✅ 宝塔面板已安装（建议 7.0+）
- ✅ Go 语言 1.21+（需要安装）
- ✅ Node.js 16+（用于前端构建）
- ✅ Nginx（用于反向代理）

---

## 🚀 部署步骤

### 第一步：安装 Go 语言

#### 方法一：通过宝塔面板安装（推荐）

1. 登录宝塔面板
2. **软件商店** → **运行环境** → 搜索 **Go**
3. 如果没有，点击 **安装**（选择最新版本）

**⚠️ 重要：安装后需要配置 PATH**

宝塔面板安装的 Go 通常位于 `/usr/local/btgojdk/go*/bin/`，需要手动添加到 PATH：

```bash
# 查找 Go 安装路径
find /usr/local -name "go" -type f 2>/dev/null | grep bin/go

# 如果找到路径（例如：/usr/local/btgojdk/go1.25.0/bin/go）
# 则添加对应的 bin 目录到 PATH
export PATH=$PATH:/usr/local/btgojdk/go1.25.0/bin

# 永久配置（添加到 ~/.bashrc）
echo 'export PATH=$PATH:/usr/local/btgojdk/go1.25.0/bin' >> ~/.bashrc
source ~/.bashrc

# 验证安装
go version
```

#### 方法二：手动安装

通过 SSH 连接到服务器，执行：

```bash
# 下载 Go 1.21.5
cd /tmp
wget https://go.dev/dl/go1.21.5.linux-amd64.tar.gz

# 解压到 /usr/local
tar -C /usr/local -xzf go1.21.5.linux-amd64.tar.gz

# 添加到 PATH
echo 'export PATH=$PATH:/usr/local/go/bin' >> ~/.bashrc
source ~/.bashrc

# 验证安装
go version
```

### 第二步：安装 Node.js（用于前端构建）

**⚠️ 重要：Node.js 版本要求**

- **最低版本**：Node.js 18.0.0（必需）
- **推荐版本**：Node.js 18.x 或 20.x
- **原因**：Vite 5.x 和项目依赖需要 Node.js 18+

#### 方法一：通过宝塔面板安装（推荐）

1. **软件商店** → **运行环境** → 搜索 **Node.js**
2. 安装 **Node.js 版本管理器**
3. **必须安装 Node.js 18.x 或更高版本**（不要安装 14.x 或 16.x，版本过低）

**⚠️ 重要：安装后需要配置 PATH**

宝塔面板安装的 Node.js 通常位于 `/www/server/nodejs/版本号/bin/`，需要手动添加到 PATH：

```bash
# 查找 Node.js 安装路径
find /www/server -name "node" -type f 2>/dev/null | grep bin/node

# 如果找到路径（例如：/www/server/nodejs/v18.17.0/bin/node）
# 则添加对应的 bin 目录到 PATH
export PATH=$PATH:/www/server/nodejs/v18.17.0/bin

# 永久配置（添加到 ~/.bashrc）
echo 'export PATH=$PATH:/www/server/nodejs/v18.17.0/bin' >> ~/.bashrc
source ~/.bashrc

# 验证安装
node -v
npm -v
```

#### 方法二：手动安装

```bash
# 使用 NodeSource 仓库安装（Debian/Ubuntu）
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt-get install -y nodejs

# 验证安装
node -v
npm -v
```

### 第三步：编译后端服务

通过 SSH 连接到服务器，执行：

```bash
cd /www/wwwroot/dy.moneyfly.top

# 设置 Go 环境变量
# 如果通过宝塔面板安装，使用：
export PATH=$PATH:/usr/local/btgojdk/go1.25.0/bin
# 如果手动安装到 /usr/local，使用：
# export PATH=$PATH:/usr/local/go/bin

# 验证 Go 是否可用
go version

# 安装依赖
go mod download
go mod tidy

# 编译后端
go build -o server ./cmd/server/main.go

# 检查编译结果
ls -lh server
```

如果编译成功，会看到 `server` 可执行文件。

### 第四步：构建前端

```bash
cd /www/wwwroot/dy.moneyfly.top/frontend

# 设置 Node.js 环境变量（如果通过宝塔面板安装）
# 请将路径替换为您实际的 Node.js 安装路径
export PATH=$PATH:/www/server/nodejs/v18.17.0/bin

# 验证 Node.js 是否可用
node -v
npm -v

# 安装前端依赖
npm install --legacy-peer-deps

# 构建生产版本
npm run build

# 检查构建结果
ls -lh dist/
```

构建完成后，前端文件会在 `frontend/dist/` 目录中。

### 第五步：配置环境变量

在项目根目录创建 `.env` 文件：

```bash
cd /www/wwwroot/dy.moneyfly.top
nano .env
```

添加以下内容（根据实际情况修改）：

```env
# 服务器配置
HOST=0.0.0.0
PORT=8000
DEBUG=false

# 数据库配置（SQLite）
DATABASE_URL=sqlite:///./cboard.db

# JWT 配置（请修改为强密码）
SECRET_KEY=your-strong-secret-key-here-change-me-to-32-bytes-minimum

# CORS 配置（添加您的域名）
BACKEND_CORS_ORIGINS=https://dy.moneyfly.top,http://dy.moneyfly.top

# 项目配置
PROJECT_NAME=CBoard Go
VERSION=1.0.0
API_V1_STR=/api/v1

# 邮件配置（可选）
SMTP_HOST=smtp.qq.com
SMTP_PORT=587
SMTP_USERNAME=your-email@qq.com
SMTP_PASSWORD=your-smtp-password
SMTP_FROM_EMAIL=your-email@qq.com
SMTP_FROM_NAME=CBoard Modern
SMTP_ENCRYPTION=tls

# 上传目录
UPLOAD_DIR=uploads
MAX_FILE_SIZE=10485760

# 定时任务
DISABLE_SCHEDULE_TASKS=false
```

**重要：** 请修改 `SECRET_KEY` 为一个强随机字符串（至少 32 字节）。

### 第六步：创建网站

1. **网站** → **添加站点**
2. **域名**：`dy.moneyfly.top`
3. **根目录**：`/www/wwwroot/dy.moneyfly.top/frontend/dist`
4. **PHP 版本**：纯静态（不需要 PHP）
5. 点击 **提交**

### 第七步：配置 Nginx 反向代理

1. 点击网站右侧的 **设置**
2. 进入 **配置文件** 标签
3. 替换为以下配置：

**⚠️ 重要：** 配置中必须包含 `#error_page 404/404.html;` 这一行，这是宝塔面板 SSL 自动部署的标识，请勿删除！

```nginx
server {
    listen 80;
    server_name dy.moneyfly.top;
    
    # 前端静态文件
    root /www/wwwroot/dy.moneyfly.top/frontend/dist;
    index index.html;

    # SSL 证书验证路径（Let's Encrypt 文件验证）
    # 宝塔面板会在网站根目录创建验证文件，所以需要指向项目根目录
    location /.well-known/acme-challenge/ {
        root /www/wwwroot/dy.moneyfly.top;
        allow all;
        access_log off;
        log_not_found off;
        default_type text/plain;
    }
    
    # 通用 .well-known 路径
    location /.well-known/ {
        root /www/wwwroot/dy.moneyfly.top;
        allow all;
        access_log off;
        log_not_found off;
    }

    # 前端路由（Vue Router）
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 后端 API 代理
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # 健康检查
    location /health {
        proxy_pass http://127.0.0.1:8000/health;
        proxy_set_header Host $host;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # 日志
    access_log /www/wwwlogs/dy.moneyfly.top.log;
    error_log /www/wwwlogs/dy.moneyfly.top.error.log;
}
```

4. 点击 **保存**

**⚠️ 重要：** 配置中必须包含 `#error_page 404/404.html;` 这一行，这是宝塔面板 SSL 自动部署的标识，请勿删除！

### 第八步：配置 SSL 证书

**⚠️ 重要提示：**

如果配置文件中没有 `#error_page 404/404.html;` 标识，宝塔面板无法自动部署 SSL 证书。请确保第七步的配置已正确添加此标识。

#### 方法一：自动部署（推荐）

**⚠️ 重要：申请证书时不要勾选"强制 HTTPS"！**

正确的申请流程：

1. **网站** → 选择 `dy.moneyfly.top` → **设置**
2. **SSL** → **Let's Encrypt** → **申请**
3. **不要勾选"强制 HTTPS"**（等证书申请成功后再开启）
4. 验证方式选择 **文件验证**
5. 点击 **提交** 申请证书

6. 证书申请成功后，再回到 **SSL** 页面
7. 勾选 **强制 HTTPS**
8. 点击 **保存**

**为什么不能先开启强制HTTPS？**

如果先开启强制HTTPS，HTTP请求会被重定向到HTTPS，但此时HTTPS还没有证书，导致Let's Encrypt无法通过HTTP访问验证文件，验证会失败。

**正确的顺序：**
1. 先申请证书（不开启强制HTTPS）
2. 证书申请成功后
3. 再开启强制HTTPS

**开启强制 HTTPS 的方法：**

证书申请成功后，有两种方式开启强制 HTTPS：

#### 方式一：使用宝塔面板的强制 HTTPS 功能（推荐）

1. **网站** → 选择 `dy.moneyfly.top` → **设置** → **SSL**
2. 勾选 **强制 HTTPS**
3. 点击 **保存**

**⚠️ 如果勾选后自动取消，说明配置文件中缺少重定向配置，请使用方法二修复。**

#### 方式二：手动修改配置文件（如果方式一无效）

**问题原因：**
宝塔面板会检测配置文件，如果 HTTP server 块中没有 `return 301 https://` 重定向配置，会自动取消"强制 HTTPS"的勾选。

**解决方法：**

1. 打开 **网站** → 选择 `dy.moneyfly.top` → **设置** → **配置文件**
2. 找到 HTTP server 块（`server { listen 80; ... }`）中的 `location / {` 这一块
3. 将：
   ```nginx
   location / {
       try_files $uri $uri/ /index.html;
   }
   ```
   修改为：
   ```nginx
   location / {
       return 301 https://$server_name$request_uri;
   }
   ```
4. **重要：** 确保 `.well-known` 路径的配置在 `location /` 之前，这样验证文件不会被重定向
5. 保存配置并重载 Nginx
6. 回到 **SSL** 页面，再次勾选 **强制 HTTPS**，这次应该可以保持勾选状态

**完整的 HTTP server 块配置示例（已启用强制 HTTPS）：**

```nginx
server {
    listen 80;
    server_name dy.moneyfly.top;
    
    root /www/wwwroot/dy.moneyfly.top/frontend/dist;
    index index.html;

    # SSL 证书验证路径（必须在最前面，优先级最高）
    location /.well-known/acme-challenge/ {
        root /www/wwwroot/dy.moneyfly.top;
        allow all;
        access_log off;
        log_not_found off;
        default_type text/plain;
    }
    
    location /.well-known/ {
        root /www/wwwroot/dy.moneyfly.top;
        allow all;
        access_log off;
        log_not_found off;
    }

    # 强制 HTTPS 重定向（排除 .well-known 路径）
    location / {
        return 301 https://$server_name$request_uri;
    }

    # 其他 location 也可以重定向，或者删除（因为都会被重定向）
    location /api/ {
        return 301 https://$server_name$request_uri;
    }

    location /health {
        return 301 https://$server_name$request_uri;
    }

    access_log /www/wwwlogs/dy.moneyfly.top.log;
    error_log /www/wwwlogs/dy.moneyfly.top.error.log;
}
```

**重要提示：**
- `.well-known` 路径必须在 `location /` 之前，确保优先级更高
- 这样配置后，证书续期时仍然可以通过 HTTP 访问验证文件
- 其他所有 HTTP 请求都会被重定向到 HTTPS
- 配置保存后，宝塔面板检测到重定向配置，就会保持"强制 HTTPS"的勾选状态

**⚠️ 如果 HTTPS 显示其他网站的内容：**

如果访问 `http://dy.moneyfly.top` 跳转到 `https://dy.moneyfly.top` 后显示的是其他网站的内容，说明 HTTPS server 块配置有问题。请检查：

1. **检查是否有多个 HTTPS server 块冲突**
   - 打开其他网站的配置文件，检查是否有 `listen 443` 的 server 块
   - 确保每个网站的 HTTPS server 块都有正确的 `server_name`

2. **确保 HTTPS server 块配置正确**
   - HTTPS server 块必须包含正确的 `server_name dy.moneyfly.top`
   - 必须指向正确的 `root` 目录：`/www/wwwroot/dy.moneyfly.top/frontend/dist`

3. **如果不想使用 HTTPS，可以禁用跳转**
   - 在 HTTP server 块中，不要添加 `return 301 https://` 重定向
   - 保持 `try_files $uri $uri/ /index.html;` 配置
   - 这样网站只通过 HTTP 访问，不会跳转到 HTTPS

如果提示"无法确定SSL配置添加位置"或其他错误，请检查配置文件中是否包含 `.well-known/acme-challenge/` 路径配置。

#### 方法二：手动部署（如果自动部署失败）

如果自动部署失败，可以手动添加 SSL 配置：

1. **网站** → 选择 `dy.moneyfly.top` → **设置** → **SSL**
2. 申请证书后，点击 **部署**
3. 如果部署失败，手动编辑配置文件，在 HTTP server 块后添加 HTTPS 配置（见下方示例）

**手动添加 HTTPS 配置示例：**

在现有的 `server { listen 80; ... }` 块后添加：

```nginx
# HTTPS 配置
server {
    listen 443 ssl http2;
    server_name dy.moneyfly.top;
    
    # SSL 证书（宝塔面板会自动配置路径）
    ssl_certificate /www/server/panel/vhost/cert/dy.moneyfly.top/fullchain.pem;
    ssl_certificate_key /www/server/panel/vhost/cert/dy.moneyfly.top/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384';
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;
    
    # 前端静态文件
    root /www/wwwroot/dy.moneyfly.top/frontend/dist;
    index index.html;

    # 前端路由（Vue Router）
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 后端 API 代理
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # 健康检查
    location /health {
        proxy_pass http://127.0.0.1:8000/health;
        proxy_set_header Host $host;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # 日志
    access_log /www/wwwlogs/dy.moneyfly.top.log;
    error_log /www/wwwlogs/dy.moneyfly.top.error.log;
}

# HTTP 重定向到 HTTPS（证书申请成功后，宝塔面板会自动添加此配置）
# 注意：如果手动添加，需要排除 .well-known 路径，否则证书续期会失败
#server {
#    listen 80;
#    server_name dy.moneyfly.top;
#    
#    # 允许 SSL 验证路径通过 HTTP 访问（证书续期需要）
#    location /.well-known/acme-challenge/ {
#        root /www/wwwroot/dy.moneyfly.top;
#        allow all;
#    }
#    
#    # 其他请求重定向到 HTTPS
#    location / {
#        return 301 https://$server_name$request_uri;
#    }
#}
```

### 第九步：配置进程守护（重要）

1. **软件商店** → **系统工具** → **Supervisor 管理器**
2. 如果没有，先安装 **Supervisor 管理器**
3. 点击 **添加守护进程**
4. 填写以下信息：
   - **名称**：`cboard-backend`
   - **启动用户**：`root`
   - **运行目录**：`/www/wwwroot/dy.moneyfly.top`
   - **启动命令**：`/www/wwwroot/dy.moneyfly.top/server`
   - **进程数量**：`1`
5. 点击 **确定**

### 第十步：启动服务

1. 在 **Supervisor 管理器** 中找到 `cboard-backend`
2. 点击 **启动**
3. 检查状态，确保显示 **运行中**

### 第十一步：创建管理员账户

在服务器上执行以下命令创建管理员账户：

```bash
cd /www/wwwroot/dy.moneyfly.top

# 方法一：使用脚本创建（推荐）
go run scripts/create_admin.go

# 或者指定用户名、邮箱和密码
go run scripts/create_admin.go admin admin@example.com your-password

# 方法二：使用环境变量设置密码
ADMIN_PASSWORD=your-strong-password go run scripts/create_admin.go
```

**默认管理员账户信息：**
- 用户名：`admin`
- 邮箱：`admin@example.com`
- 密码：`admin123`（如果未指定）

**⚠️ 重要：** 创建后请立即登录并修改密码！

### 第十二步：测试访问

1. 访问：`http://dy.moneyfly.top`（或 `https://dy.moneyfly.top`）
2. 检查后端 API：`http://dy.moneyfly.top/api/v1/health`
3. 访问管理员登录页面：`http://dy.moneyfly.top/admin/login`
4. 使用创建的管理员账户登录

---

## 🔧 常见问题

### 1. Go 命令找不到（go: command not found）

**问题：** 执行 `go mod tidy` 或 `go build` 时提示 `go: command not found`

**原因：** Go 已安装但 PATH 环境变量未配置

**解决方法：**

```bash
# 1. 查找 Go 安装位置
find /usr/local -name "go" -type f 2>/dev/null | grep bin/go

# 2. 如果通过宝塔面板安装（通常在 /usr/local/btgojdk/go*/bin/）
# 临时设置（当前会话有效）
export PATH=$PATH:/usr/local/btgojdk/go1.25.0/bin

# 3. 永久配置
echo 'export PATH=$PATH:/usr/local/btgojdk/go1.25.0/bin' >> ~/.bashrc
source ~/.bashrc

# 4. 验证
go version
```

**注意：** 请将 `/usr/local/btgojdk/go1.25.0/bin` 替换为您实际找到的路径。

### 2. Node.js/npm 命令找不到（npm: command not found）

**问题：** 执行 `npm install` 或 `npm run build` 时提示 `npm: command not found` 或 `node: command not found`

**原因：** Node.js 已安装但 PATH 环境变量未配置

**解决方法：**

```bash
# 1. 查找 Node.js 安装位置
find /www/server -name "node" -type f 2>/dev/null | grep bin/node

# 2. 如果通过宝塔面板安装（通常在 /www/server/nodejs/版本号/bin/）
# 临时设置（当前会话有效）
export PATH=$PATH:/www/server/nodejs/v18.17.0/bin

# 3. 永久配置
echo 'export PATH=$PATH:/www/server/nodejs/v18.17.0/bin' >> ~/.bashrc
source ~/.bashrc

# 4. 验证
node -v
npm -v
```

**注意：** 请将 `/www/server/nodejs/v18.17.0/bin` 替换为您实际找到的路径。

**⚠️ 版本要求：** Node.js 必须是 18.0.0 或更高版本。如果您的版本是 14.x 或 16.x，请升级：

```bash
# 通过宝塔面板升级 Node.js
# 1. 软件商店 → Node.js 版本管理器
# 2. 安装 Node.js 18.x 或 20.x
# 3. 更新 PATH 配置为新版本路径
```

**常见错误：** 如果看到 `SyntaxError: Unexpected token '??='` 或类似错误，说明 Node.js 版本过低，必须升级到 18+。

### 3. 后端无法启动

**检查日志：**
```bash
cd /www/wwwroot/dy.moneyfly.top
tail -50 server.log
```

**常见原因：**
- 端口 8000 被占用：`netstat -tlnp | grep 8000`
- 数据库文件权限问题：`chmod 644 cboard.db`
- 环境变量未配置：检查 `.env` 文件

### 4. 前端无法访问

**检查：**
- Nginx 配置是否正确
- 前端文件是否构建成功：`ls -lh frontend/dist/`
- Nginx 错误日志：`tail -50 /www/wwwlogs/dy.moneyfly.top.error.log`

### 5. API 请求失败

**检查：**
- 后端是否运行：`ps aux | grep server`
- 反向代理配置是否正确
- CORS 配置是否包含域名

### 6. 数据库权限问题

```bash
cd /www/wwwroot/dy.moneyfly.top
chmod 644 cboard.db
chown www:www cboard.db
```

### 6. 无法登录管理员账户

**问题：** 无法登录管理员账户，提示"用户名或密码错误"或其他错误

**可能的原因和解决方法：**

#### 原因 1：管理员账户不存在

**解决方法：** 创建管理员账户

```bash
cd /www/wwwroot/dy.moneyfly.top

# 确保 Go 在 PATH 中
export PATH=$PATH:/usr/local/btgojdk/go1.25.0/bin

# 创建管理员账户（使用默认密码 admin123）
go run scripts/create_admin.go

# 或者指定自定义密码
go run scripts/create_admin.go admin admin@example.com your-password
```

**默认账户信息：**
- 用户名：`admin`
- 邮箱：`admin@example.com`
- 密码：`admin123`（如果未指定）

#### 原因 2：使用了错误的登录页面

**⚠️ 重要：** 管理员账户必须使用**管理员登录页面**登录，不能使用普通用户登录页面。

- ✅ **正确：** 访问 `http://dy.moneyfly.top/admin/login`（管理员登录页面）
- ❌ **错误：** 访问 `http://dy.moneyfly.top/login`（普通用户登录页面，管理员无法在此登录）

#### 原因 3：密码错误

**解决方法：** 重置管理员密码

```bash
cd /www/wwwroot/dy.moneyfly.top

# 重新创建管理员账户（会更新密码）
go run scripts/create_admin.go admin admin@example.com new-password
```

#### 原因 4：账户状态问题

检查账户是否被禁用或未验证：

```bash
# 使用 SQLite 命令行检查
cd /www/wwwroot/dy.moneyfly.top
sqlite3 cboard.db "SELECT id, username, email, is_admin, is_active, is_verified FROM users WHERE username='admin' OR email='admin@example.com';"
```

如果 `is_active` 为 0 或 `is_verified` 为 0，需要修复：

```bash
sqlite3 cboard.db "UPDATE users SET is_active=1, is_verified=1, is_admin=1 WHERE username='admin' OR email='admin@example.com';"
```

#### 原因 5：后端服务未运行

检查后端服务是否正常运行：

```bash
# 检查服务状态
systemctl status cboard

# 或者检查进程
ps aux | grep server

# 检查日志
tail -50 /www/wwwroot/dy.moneyfly.top/server.log
# 或
journalctl -u cboard -n 50
```

#### 原因 6：API 请求失败

检查后端 API 是否可访问：

```bash
# 测试健康检查接口
curl http://127.0.0.1:8000/api/v1/health

# 测试登录接口
curl -X POST http://127.0.0.1:8000/api/v1/auth/login-json \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

**完整的排查步骤：**

1. **确认管理员账户存在：**
   ```bash
   cd /www/wwwroot/dy.moneyfly.top
   export PATH=$PATH:/usr/local/btgojdk/go1.25.0/bin
   go run scripts/create_admin.go
   ```

2. **确认使用正确的登录页面：**
   - 访问：`http://dy.moneyfly.top/admin/login`
   - 不要使用：`http://dy.moneyfly.top/login`

3. **确认后端服务运行：**
   ```bash
   systemctl status cboard
   # 或
   ps aux | grep server
   ```

4. **检查浏览器控制台错误：**
   - 按 F12 打开开发者工具
   - 查看 Console 和 Network 标签
   - 查看是否有错误信息

5. **检查后端日志：**
   ```bash
   tail -100 /www/wwwroot/dy.moneyfly.top/server.log
   # 或
   journalctl -u cboard -n 100
   ```

---

## 📝 后续维护

### 更新代码

```bash
cd /www/wwwroot/dy.moneyfly.top

# 1. 停止服务（在 Supervisor 中停止）
# 2. 更新代码
git pull  # 或重新上传文件

# 3. 重新编译后端
go build -o server ./cmd/server/main.go

# 4. 重新构建前端
cd frontend
npm run build

# 5. 启动服务（在 Supervisor 中启动）
```

### 查看日志

```bash
# 后端日志
tail -f /www/wwwroot/dy.moneyfly.top/server.log

# Nginx 访问日志
tail -f /www/wwwlogs/dy.moneyfly.top.log

# Nginx 错误日志
tail -f /www/wwwlogs/dy.moneyfly.top.error.log
```

### 重启服务

在 **Supervisor 管理器** 中：
1. 找到 `cboard-backend`
2. 点击 **重启**

---

## ✅ 部署检查清单

- [ ] Go 语言已安装（`go version`）
- [ ] Node.js 已安装（`node -v`）
- [ ] 后端编译成功（`./server` 文件存在）
- [ ] 前端构建成功（`frontend/dist/` 目录存在）
- [ ] `.env` 文件已配置
- [ ] 网站已创建
- [ ] Nginx 配置已更新
- [ ] SSL 证书已配置
- [ ] Supervisor 守护进程已配置并运行
- [ ] 网站可以正常访问
- [ ] API 可以正常访问

---

## 🎉 完成！

如果所有步骤都完成，您的网站应该可以正常访问了！

如有问题，请查看日志文件或联系技术支持。

